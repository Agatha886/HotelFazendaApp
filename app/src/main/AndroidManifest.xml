<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.unip.pimIV.hotelFazenda">

    <application
        android:icon="@mipmap/ic_launcher_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_app_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HotelFazenda">
        <activity android:name=".ui.activity.CompraComcluida" />
        <activity android:name=".ui.activity.PagamentoActvity" />
        <activity android:name=".ui.activity.CadastroHospedagemActivity" />
        <activity android:name=".ui.activity.MainActivity" />
        <activity android:name=".ui.activity.FormularioCadastroActivity" />
                <activity android:name=".ui.activity.LoginActivity"
                    android:exported="true">
                    <intent-filter>
                        <action android:name="android.intent.action.MAIN" />

                        <category android:name="android.intent.category.LAUNCHER" />
                    </intent-filter>
                </activity>

    </application>

</manifest>